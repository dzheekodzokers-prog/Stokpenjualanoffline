workflows:
  android-build:
    name: Cek Isi Zip
    environment:
      java: 17
    scripts:
      - name: Unzip Project
        script: unzip StokPenjualanOffline-Android.zip
      - name: List All Files
        script: find . -type f | sort
    artifacts:
      - 'codemagic.yaml'  # ini cuma biar gak error
